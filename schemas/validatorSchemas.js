class ValidatorTest {
    constructor (tipoDeTest,advertencias, commands, index) {
        this.tipoDeTest = tipoDeTest
        this.advertencias = advertencias
        this.index = index
        this.commands = commands
    }
}

module.exports = { ValidatorTest }